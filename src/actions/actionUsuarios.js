import { types } from "../types/types";


export const getUsers = () => {
  return (dispatch) => {
    
 
   
  };
};

export const userSincrono = (users) => {
  return {
    type: types.getUsers,
    payload: {},
  };
};
